# 5-minutes-more

## File Hierarchy

```
./eks-deploy/
├── ingress
│   └── jenkins-ingress.yaml
├── jenkins
│   ├── init-groovy
│   │   └── seed.groovy
│   └── values.yaml
└── postgres
    ├── init-sql
    │   └── init.sql
    └── values.yaml

5 directories, 5 files
```
